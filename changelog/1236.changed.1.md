Dates are recognized only from a genuine datetime dtype, never by parsing string columns with `pd.to_datetime()`: a string column that merely looks like a date is read as a plain category or text. The never-consumed `FeatureModality.DATE` has been removed.
