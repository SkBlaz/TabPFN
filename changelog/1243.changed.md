`load_fitted_tabpfn_model` and the `load_from_fit_state` class methods now default to `device="auto"` instead of `device="cpu"`, so a GPU-fitted estimator no longer silently reloads onto CPU. Pass `device="cpu"` for the old behavior.
